rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Cada usuário só envia/lê os próprios áudios.
    match /users/{uid}/audios/{file} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
